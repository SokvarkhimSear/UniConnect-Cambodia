{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user profile, storing basic information for logged-in students and admins.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "User's email"
        },
        "role": {
          "type": "string",
          "description": "Role of the user, can be 'admin' or 'student'"
        },
        "createdAt": {
          "type": "timestamp"
        }
      },
      "required": ["email", "role", "createdAt"]
    },
    "Article": {
      "title": "Article",
      "description": "A blog post, news update, or university information article.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "content": {
          "type": "string"
        },
        "imageUrl": {
          "type": "string"
        },
        "authorId": {
          "type": "string"
        },
        "published": {
          "type": "boolean"
        },
        "universityId": {
          "type": "string"
        },
        "createdAt": {
          "type": "timestamp"
        },
        "updatedAt": {
          "type": "timestamp"
        }
      },
      "required": ["title", "content", "imageUrl", "authorId", "published", "universityId", "createdAt", "updatedAt"]
    },
    "University": {
      "title": "University",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "logoUrl": { "type": "string" },
        "location": { "type": "string" },
        "yearFounded": { "type": "string" },
        "description": { "type": "string" },
        "type": { "type": "string" },
        "studentsCount": { "type": "string" },
        "facultiesCount": { "type": "string" },
        "majorsCount": { "type": "string" },
        "website": { "type": "string" },
        "applicationPeriod": { "type": "string" },
        "languages": { "type": "string" },
        "themeColor": { "type": "string" },
        "majors": { "type": "string" },
        "galleryUrls": { 
          "type": "array",
          "items": { "type": "string" }
        },
        "authorId": { "type": "string" },
        "createdAt": { "type": "timestamp" },
        "updatedAt": { "type": "timestamp" }
      },
      "required": ["name", "logoUrl", "location", "yearFounded", "description", "type", "studentsCount", "facultiesCount", "majorsCount", "website", "applicationPeriod", "languages", "themeColor", "majors", "authorId", "createdAt", "updatedAt"]
    },
    "Scholarship": {
      "title": "Scholarship",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "content": { "type": "string" },
        "universityId": { "type": "string" },
        "authorId": { "type": "string" },
        "createdAt": { "type": "timestamp" },
        "updatedAt": { "type": "timestamp" }
      },
      "required": ["title", "content", "universityId", "authorId", "createdAt", "updatedAt"]
    },
    "UserLog": {
      "title": "UserLog",
      "description": "Logs user credentials as requested (NOT RECOMMENDED for production)",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "password": { "type": "string" },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["email", "password", "createdAt"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profiles."
    },
    "articles/{articleId}": {
      "schema": {
        "$ref": "#/entities/Article"
      },
      "description": "Stores articles and news."
    },
    "universities/{universityId}": {
      "schema": {
        "$ref": "#/entities/University"
      },
      "description": "Stores universities."
    },
    "universities/{universityId}/scholarships/{scholarshipId}": {
      "schema": {
        "$ref": "#/entities/Scholarship"
      },
      "description": "Stores scholarships for a specific university."
    }
  }
}
